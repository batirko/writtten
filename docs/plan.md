# Plan

> Phased build plan. **Check the "Current phase" marker before adding functionality.** If a thing belongs to a later phase, don't build it yet — flag it instead. Scope creep is this project's primary risk. Refine phases as you learn; keep this file the source of truth for sequencing. Field-test reviews in `docs/snapshots/` feed the **Discovered / unscheduled** backlog near the bottom — check there for insights not yet folded into a phase.

> **What "good" means:** `docs/product-requirements.md` is the fidelity bar (tiered requirements R1.1–R6.4 + the five load-bearing tensions); `docs/acceptance-testing/fidelity-criteria.md` turns it into pass/fail gates. The phases below sequence the work; those two files define when it's faithful.

**Current phase: Phase 7 — "Post-launch polish & credibility."** (**Public launch happened 2026-07-10** — writtten.com is live, the repo is public, and outreach has started. Phases 1–6 are shipped/closed. The plan was re-cut on 2026-07-10 with the owner: the old catch-all "Phase 7 — Later/optional" was split into **Phase 7 — post-launch polish** (finish what a public visitor/reader now touches), **Phase 8 — validation & signal quality** (the evidence push, V1 first), and **Phase 9 — later/post-traction** (parked until the drafting habit demonstrably takes hold). Earlier re-cut history: on 2026-06-17 the experience/signal-quality/validation work that had accreted in Phase 5 became Phase 6, and the old "Phase 6 — Later/optional" became Phase 7.)

> **Routing legend.** Open milestones below carry an annotation `— <readiness> <complexity> · <agent>` so it's clear what's ready, how hard, and who should build it. Completed (`[x]`) lines are left unannotated.
>
> - **Readiness:** 🟢 fully defined, ready to build · 🟡 mostly defined, decisions along the way · 🟠 not defined, needs pre-work/planning · 🔴 concept only, no design.
> - **Complexity:** Low · Med · High.
> - **Agent:** 🧠 capable/expensive (judgment, design, prompt-quality, architecture) · ⚙️ mid · 🔧 simple/mechanical (well-specified, low-decision).
>
> **This is required metadata, not decoration.** Every new open milestone (here or in any phase section) gets the annotation **on creation** — there is no unannotated open item. And it's **living:** whenever work touches an item, re-assess and update its annotation in the same change. **Readiness** moves the most (e.g. a design spec lands → 🟠→🟡, or a milestone is fully specced → 🟡→🟢); **complexity** and **agent** shift less often but get re-rated when new information changes the picture (e.g. a "simple" item turns out to need architectural judgment → 🔧→🧠). When an item is completed, drop the annotation as the `[x]` is added.

> **Milestone anatomy — the default entry style.** The **Phase 6** entries (now in [`plan-archive.md`](plan-archive.md)) are the template: a plan item is a **living record of a decision**, not a one-line to-do. Write new milestones — and grow existing ones as work lands — from as many of these parts as apply, in this order:
>
> 1. **Bold title**, carrying any tracking id(s): `**Widen the section check to same-section pairs (OBS-033/UX-018)**`.
> 2. **Provenance** — how and when it surfaced, as an **absolute date**: `field-discovered 2026-07-08`, `decided 2026-06-18 (with the user)`, `field-observed (owner)`.
> 3. **The problem** — what's wrong and why it matters, concretely: name the real session/example that exposed it and the `file.ts:line` if known.
> 4. **The decision** — a **bold state marker + date** naming the chosen mechanism (and the instructive rejected alternative): `**Decided 2026-07-09 (mechanism A):** …`. The markers trace an arc a live entry walks: **Field-discovered → Decided / Design settled → Shipped**.
> 5. **Implementation specifics** — the exact files, functions, and tokens in `code`, so the entry is self-locating without opening the PR.
> 6. **Verification** — the guard that keeps it fixed: the ratchet fixture, unit test, or live-verify.
> 7. **Cross-references** — trailing `→ see docs/projects/<file>.md` (the design home) and `· resolves OBS-NNN / UX-NNN`.
> 8. **Routing annotation** (open items only) — the four routing axes defined by the legend above, appended as `— <readiness> <complexity> · <agent>` plus, in a parallel-lane phase, `· Lane: <name>`. Spell out all four:
>    - **Readiness** — how defined / ready-to-build the work is: 🟢 ready · 🟡 mostly, decisions along the way · 🟠 needs pre-work/planning · 🔴 concept only.
>    - **Estimated size** (complexity) — Low · Med · High (compound forms like `Med–High` or `High-decision/Low-build` are fine).
>    - **LLM capacity** (agent) — who should build it: 🧠 capable/expensive (judgment, design, prompt-quality, architecture) · ⚙️ mid · 🔧 simple/mechanical, low-decision.
>    - **Lane** — `· Lane: <name>` where the phase runs parallel work lanes; it names the code territory the item lives in (see the archived Phase 6 → _Parallel work lanes_ in [`plan-archive.md`](plan-archive.md)).
>
>    The annotation is **required on every open item and living** — re-rate it whenever work touches the item — and **dropped** when the item is checked `[x]`.
>
> Not every item needs all eight — a trivial mechanical fix can stay terse — but the **default** is the fuller Phase-6 form, and a completed item **keeps** its record inline (see the archived Phase 6) rather than being deleted. Keep any multi-line notes or example lines inside a blockquote (like this one) so the routing-annotation contract doesn't read them as milestones.

---

## Projects index

> **Completeness contract:** every file in `docs/projects/` appears in this table — if it isn't listed here, it doesn't exist. When you create a project file you **must** add a row here (minimum); if it's scoped to specific phases, **also** add `→ see` links on those milestone lines below (maximum). Status is the file's frontmatter `status:` field, mirrored here — never encoded in the filename. The table is **grouped by the file's `kind:` frontmatter field** (`spec` · `quality` · `infra` · `research`) under the sub-headers below; a file's row must sit under the sub-header matching its `kind`. `docs/projects.index.test.ts` enforces folder ↔ table ↔ frontmatter consistency (presence, status, and kind/grouping).
>
> **Living observation logs are not projects.** The append-only field logs (`prompt_quality_observations`, `ux_quality_observations`) live in `docs/logs/`, not `docs/projects/`, and are intentionally absent from this index.

### Feature & platform specs

| Project                                                                  | Status      | Phases                                                           | One-line                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [message_generation_workflow](projects/message_generation_workflow.md)   | done        | 1 ✅ · 2 ✅ · 3 ✅                                               | The contract between editor, evaluator, model router, and feed — when observations fire, what context the LLM sees, how the feed behaves.                                                                                                                                                                                                                                                                                                                           |
| [model_rotation_and_debugging](projects/model_rotation_and_debugging.md) | done        | 1 ✅ · 3 ✅ (Ollama skipped)                                     | Gemini free-tier rate-limit resiliency: call batching, model rotation, cool-down registry, LLM debug panel.                                                                                                                                                                                                                                                                                                                                                         |
| [section_as_eval_unit](projects/section_as_eval_unit.md)                 | done        | 4                                                                | Redesign the evaluation unit from individual ProseMirror blocks to semantic sections (heading + body), unifying typing and paste workflows and eliminating the heading-hallucination class of bugs.                                                                                                                                                                                                                                                                 |
| [bulk_paste_evaluation](projects/bulk_paste_evaluation.md)               | done        | 4                                                                | Evaluate multi-section drafts on bulk paste/import — fast-tier span checks per section plus one ledger-internal contradiction sweep — closing the gap where a single paste went unevaluated and import fired N paid-tier calls.                                                                                                                                                                                                                                     |
| [doc_scope_reconciliation](projects/doc_scope_reconciliation.md)         | done        | 4 (best-match + grace period) · 5 (resolution-aware + decisions) | Repair document-scope observation reconciliation. Tier 1: best-match pairing + grace (2026-06-05). Tier 2A: resolution-aware doc-scope reconciliation, priorId mapping, three-pass reconciler (2026-06-06). Tier 2B: ledger sweep authoritative-with-grace on paid tier (2026-06-06).                                                                                                                                                                               |
| [egress](projects/egress.md)                                             | idea        | 5                                                                | Build-ready specs for Export (MD + print-to-PDF behind a swap seam), Copy (rich text + MD), and PWA install/offline app shell. Lean; client-side only.                                                                                                                                                                                                                                                                                                              |
| [accessibility](projects/accessibility.md)                               | idea        | 5                                                                | Itemized a11y & keyboard-first checklist for the feed and hover/highlight interaction — mechanical items plus flagged design-dependent ones.                                                                                                                                                                                                                                                                                                                        |
| [mobile_responsive](projects/mobile_responsive.md)                       | in-progress | 6 ✅ · 9                                                         | The never-designed narrow-viewport / touch dimension. Phase-6 courtesy pass shipped (don't ship a broken layout to phone visitors of the first public release); the Phase-9 mobile review companion (the real touch model for span↔card, reframed as feed-first review, not a cramped editor) is parked post-traction.                                                                                                                                                                                 |
| [visual_style](projects/visual_style.md)                                 | done        | 6                                                                | The visual design system — type, colour (OKLCH tokens), spacing, elevation, motion, per-component language. Calm/editorial: serif writing canvas, sans feed, ink-indigo brand accent on warm paper. Light-first; dark deferred.                                                                                                                                                                                                                                     |
| [feed_surface](projects/feed_surface.md)                                 | done        | 6                                                                | De-sidecar the observation feed into a "companion surface" — reflowing column of borderless floating cards, a unified bottom-right control center (actions ← / process ↑), a floating settings modal, an icon-only activity+model dot, a document-attached context affordance, an elegant archive, and a reserved filter seam. Preserves kind×severity (tag-carries-colour, no left stripe); reuses the visual_style tokens and ui_interaction_mechanics contracts. |
| [ui_interaction_mechanics](projects/ui_interaction_mechanics.md)         | idea        | 6                                                                | Canonical interaction contracts for feed↔editor — hover/focus highlight, click-scrolls-to-span, dismiss + Undo toast (with suppression rollback), card anatomy, the two "also noticed" drawers. Makes partly-built Phase-4 mechanics intentional; scopes against R7b/R3c.                                                                                                                                                                                           |
| [onboarding_first_run](projects/onboarding_first_run.md)                 | done        | 6                                                                | First-run welcome + "See it in action" example + quiet empty states. Reopened 2026-07-07 (§ Revision) for keyless activation — blocking welcome modal naming the key requirement, mock reachable, standing "add your key" card — and shipped; closed 2026-07-10.                                                                                                      |
| [editor_formatting_ux](projects/editor_formatting_ux.md)                 | done        | 6                                                                | Discoverable formatting for the writing canvas — a selection bubble menu (inline marks + links) + a slash menu (block types), appear-on-demand with zero standing chrome, + a Link extension. Closes UX-004: formatting works but is invisible.                                                                                                                                                                                                                     |
| [archive_trust](projects/archive_trust.md)                               | idea        | 6                                                                | R3b — persist each observation's ghost-anchor text + explicit closure reason and render them on archive cards so the archive is trustworthy.                                                                                                                                                                                                                                                                                                                        |
| [canvas_content_types](projects/canvas_content_types.md)                 | in-progress | 6 ✅ · 9                                                         | Decide & bound which rich-content types the canvas accepts on paste / lets the user create (tables, images, links, task lists) and how each interacts with the section/eval model. Phase-6 scope shipped (tables + the degradation contract); the long-tail paste inventory, table harness seeding, and image-storage decision are parked in Phase 9.                                                                                                                                                                                           |
| [table_editing_controls](projects/table_editing_controls.md)             | done        | 6                                                                | Structural editing controls for tables (add/remove row & column, delete table) — shipped Option A, a floating in-table bubble-menu-style pill wiring the `@tiptap/extension-table` commands; appears only when the caret is in a table, deletes the whole table on last-row/column removal, keeps eval-inertness.                                                                                                                                                   |
| [doc_scope_legibility](projects/doc_scope_legibility.md)                 | done        | 6                                                                | Make document-scoped (unanchored) observations legible as such in the feed — a card-intrinsic scope marker ("Whole doc" chip) + honest affordances (drop the click-to-locate / hover-to-highlight that silently no-ops on a card with no span). Built & verified 2026-07-09; repairs the lost UX-008 "Whole document" treatment; complements (does not reopen) the dropped R4 doc-level anchoring.                                                                  |

### Signal & philosophy quality

| Project                                                                            | Status      | Phases                   | One-line                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------------------------------- | ----------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [evaluation_signal_quality](projects/evaluation_signal_quality.md)                 | done        | 1 · 2 · 3 (remediation)  | Signal-to-noise findings from a real PRD paste-test — heading-only blocks hallucinate, the ledger self-pollutes, free-tier "strong" checks run on a weak model and emit confident false contradictions, observations duplicate — remediated in Chunk 1.                                                                                                                     |
| [observation_taxonomy_and_priority](projects/observation_taxonomy_and_priority.md) | in-progress | 4 (A·B·E ✅) · 9 (C·D)   | Extend observations with kind/severity/confidence/priority axes, close the decision-rigor taxonomy gap, add a client-side reflection mirror kind, and introduce a budget-based noisiness model in the feed.                                                                                                                                                                 |
| [philosophy_guardrails](projects/philosophy_guardrails.md)                         | done        | 4 (G1·G2 ✅) · 6 (G3·G4 ✅) | The three formerly-unguarded qualitative guardrails — flattery-resistant dismissal, explicit anti-taxonomy, no-disguised-fix register — plus a discomfort-budget ceiling. Enforces the qualitative half of the fidelity bar in code + CI.                                                                                                                                            |
| [smart_feed_curation](projects/smart_feed_curation.md)                             | idea        | 9                        | Resolve the zero-config "smart feed" vs. user desire for filters/sorting/"top 5" (UX-010, R2c). Decision: maturity-aware curation + exactly one three-step noisiness switch; per-type filters/sorting/manual "top N" refused so the feed never becomes a settings dashboard.                                                                                                |
| [maturity_aware_severity](projects/maturity_aware_severity.md)                     | done        | 6                        | Severity _and message voice_ as a function of document maturity (the R2 principle, graduated into its own milestone): structural gaps surface as soft "opportunities" early and promote to firmer "warnings" — kind + severity + voice — as the doc matures, by a deterministic structural proxy. Defects always surface. Unblocks R2c.                                     |
| [emotional_register](projects/emotional_register.md)                               | done        | 6                        | Persona spec (trusted senior colleague), wrong-persona anti-patterns, message voice guide, and tone as a labeled eval dimension. The felt-tone half of register discipline.                                                                                                                                                                                                 |
| [section_eval_precision](projects/section_eval_precision.md)                       | done        | 6                        | Precision costs of section-isolation, all shipped: intra-section conflicts surface via single-block anchoring (OBS-026); span checks get sibling summaries + ledger + heading-intent as established context (OBS-027); bodyless-heading sections are inert (OBS-029). Two out-of-floor follow-ups deferred.                                                                 |
| [document_type_calibration](projects/document_type_calibration.md)                 | in-progress | 6 ✅ · 8                 | Make the Document Context / Stage field a first-class calibrator of evaluation — recalibrate strictness and which checks apply by document class — so PRD-grade citation/structure expectations don't fire on essays, blogs, or memos. The mechanism that lets scope widen from "PRDs" to "documents people write for work" without a taxonomy explosion (OBS-028/OBS-023). |
| [contradiction_coverage](projects/contradiction_coverage.md)                       | done        | 6                        | Closed the intra-section contradiction gap: mechanism A (same-section pairs folded into the per-section check from in-memory `extractedClaims`, ≥2-claim gate, `conflictPairKey` dedup) shipped 2026-07-09 in #161 with the `contradiction-intra-section` ratchet fixture. The cross-section short-draft facet of UX-016 (paste-sweep maturity gate) carried to Phase 8.                            |

### Pipeline & dev infrastructure

| Project                                                            | Status      | Phases                                                                     | One-line                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [ai_tooling_integration](projects/ai_tooling_integration.md)       | idea        | 3 (LEANN deferred) · 4 (SkillOpt ratchet) · 5 (markitdown deferred)        | SkillOpt, LEANN, and markitdown — when to adopt, what each needs, and how each maps to a specific phase milestone.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| [agent_acceptance_harness](projects/agent_acceptance_harness.md)   | done        | 1 · 2                                                                      | Dev-only observability + control surface (debug state API, structured event stream, readiness signal, seedable state, mock LLM) so an agent can drive and verify acceptance tests deterministically.                                                                                                                                                                                                                                                                                                                                           |
| [evaluator_quality_ratchet](projects/evaluator_quality_ratchet.md) | done        | 4 · 6                                                                      | Labeled fixture corpus + two-tier scorer (deterministic replay CI + opt-in live precision/recall) so evaluator accuracy can't silently regress. Prerequisite for SkillOpt prompt optimization.                                                                                                                                                                                                                                                                                                                                                 |
| [debug_log](projects/debug_log.md)                                 | in-progress | 4 ✅ · 7 (redaction) · 9 (unify + token/cost)                              | Redesign the debug/observability log into one call-centric, self-describing event model — merge request+response, dereference static prompts, add archival (user + system) records, unify the two divergent logs — for human reading and AI consumption. Phase 4 slice shipped; the export-redaction check rides Phase 7 as launch hygiene; the unify + token/cost + retention slice is parked in Phase 9.                                                                                                                                                                                                                      |
| [byok_capability_model](projects/byok_capability_model.md)         | in-progress | 5 (capability decoupling ✅) · 6 (dual-key UI ✅) · 9 (multi-key rotation + management UX) | Decouple model _capability_ from the _credential_ for BYOK. Phase 5 shipped (2026-06-06): explicit `ModelCapability` descriptor threaded via EvalContext, evaluator re-gated off `paidKey`, UI key-tier toggle. Phase 6 shipped (2026-07-07): two-field Gemini free+paid key UI, surfacing the existing free→paid fallback. Phase 9 (multi-_free_-key RPD rotation in `gemini.ts` + the key-inventory / routing-transparency management UX) remains.                                                                                                                                          |
| [lifecycle_integrity](projects/lifecycle_integrity.md)             | in-progress | 5                                                                          | Code-correctness hardening (2026-06-10 code audit): repair broken build/lint gates + add CI, close three silent-failure paths in the observation lifecycle, and make `anchorText` load-bearing so dismissals hold and highlights stay truthful.                                                                                                                                                                                                                                                                                                |
| [revert_aware_evaluation](projects/revert_aware_evaluation.md)     | done        | 6                                                                          | Make the eval pipeline no-op-aware so an undone change costs (near-)nothing. Content-hash snapshot/restore shipped 2026-07-04 — a net-zero edit (toggle/undo/no-op-format) now leaves zero net observation churn. Section-boundary-commit debounce shipped 2026-07-06 — a toggle→revert now fires zero transient model calls and in-flight evals for reverted boundaries are invalidated (UX-014).                                                                                                                                             |
| [oss_launch_readiness](projects/oss_launch_readiness.md)           | idea        | 6 ✅ · 7 · 9 (Superb)                                                      | The open-source launch readiness checklist — repo hygiene (LICENSE, README pitching the inversion, community files), the pre-flight cleanup + security sweep, the docs/ transparency call, contributor on-ramp (three extension seams, a second model adapter), positioning assets, and the go-to-market/post-launch plan. Tiered Minimum → Good-enough → Superb. Resolves the "OSS: real or decorative?" open question → real.                                                                                                                |
| [multi_provider_router](projects/multi_provider_router.md)         | done        | 6                                                                          | Expand BYOK from Gemini-only to Gemini + OpenAI + Anthropic at launch: lift the Gemini-shaped resilience layer (pools, cool-down, 429/quota parsing) into a provider-agnostic `ProviderAdapter` seam, add two reference adapters, and rework the global weak/strong toggle into per-provider model selection. Only Gemini has a free tier, so the zero-config on-ramp rides on Gemini alone. First-party version of the OSS Superb "non-Gemini adapter" item. Shipped across 4 PRs 2026-07-07 (seam · adapters+registry · Settings UX · docs). |
| [hosted_proxy](projects/hosted_proxy.md)                           | idea        | 6 ✅ · 9                                                                   | Optional, capped hosted proxy so writtten.com visitors can evaluate their own writing zero-config — a serverless function holding the owner's paid Gemini key behind a hard global daily budget + per-client throttle, with transparent "N free evals then BYOK" UX. Reopens the Phase-3 "proxy stays client-side" decision (launch funnel reopens it); GO/NO-GO unresolved (owner cost call). Paid-key proxy is a privacy _upgrade_ over free BYOK.                                                                                           |
| [strong_tier_eval_reliability](projects/strong_tier_eval_reliability.md) | idea | 7 (post-launch polish) | Bound strong-tier reasoning per adapter (OpenAI `reasoning_effort`, Gemini `thinkingBudget`, Anthropic already floors), raise the shared 45s→60s request timeout, and give paid strong pools a failure-only fallback — so a slow contradiction sweep no longer times out and drops the whole call. Prompted by a live `gpt-5.5` 503. |

### Research & synthesis

| Project                                                                    | Status | Phases                                         | One-line                                                                                                                                                                                                     |
| -------------------------------------------------------------------------- | ------ | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [quality_remediation_synthesis](projects/quality_remediation_synthesis.md) | idea   | 4 (R1·R3·R5·transparency) · 6 (UX + precision) | Root-cause synthesis of the prompt- and UX-quality logs — collapses ~32 field observations into 6 cross-cutting root causes, sequences the fixes, and flags which are Phase 4 acceptance blockers.           |
| [field_validation](projects/field_validation.md)                           | idea   | 8                                              | Close the n=0 gap (2026-06-10 due-diligence audit): base-rate corpus study, external-PM sessions, and hero-miss instrumentation so the central bet becomes falsifiable. **Scheduled as the heart of Phase 8 (2026-07-10); V1 first.** |

---

## Phases 0–6 — complete (archived)

> **Shipped/closed.** Full milestone-level detail moved to [`plan-archive.md`](plan-archive.md) on 2026-07-10 to keep the live plan readable. Quick recap:
>
> - **Phase 0 — Foundation:** local-first app shell — TipTap editor, stable block ids, IndexedDB persistence, two-panel layout, stubbed model-router.
> - **Phase 1 — "The Wow":** the v0 contradiction loop that proves the inversion.
> - **Phase 2 — Full taxonomy & lifecycle:** the fixed observation taxonomy + message lifecycle and archive.
> - **Phase 3 — Models, cost & BYO key:** the model-router seam, cost controls, and bring-your-own-key.
> - **Phase 4 — Core experience:** signal quality and the calm feed (priority / severity / confidence axes).
> - **Phase 5 — Egress, install, hardening:** export/copy, installable offline PWA, accessibility, and the L1–L7 code-correctness hardening sprint.
> - **Phase 6 — Make it good: experience & signal quality** (closed 2026-07-10): the visual + companion-surface feed, the UI/UX mechanics program, onboarding + first-run activation, the mobile courtesy pass, maturity-aware severity, register discipline, fast/strong signal-precision hardening, and multi-provider BYOK. Its four still-open-at-close milestones live in Phase 7 → _Carryover from Phase 6_; the entry-style template and _Parallel work lanes_ definitions are in the archive.
>
> One hardening milestone (**L8 — editor hot-path perf**) was still open at archival; it is parked in **Phase 9** below (2026-07-10 re-cut).

---

## Phase 7 — Post-launch polish & credibility

> **Launched 2026-07-10:** writtten.com is live (Cloudflare Pages, BYOK-only first-run), the repo is public, and outreach has started. This phase finishes what a public visitor or reader now touches — the launch residuals plus the two prod-visible doc-level prompt defects pulled up from the prompt-quality log. Deliberately bounded and finishable; when it drains, Phase 8 (validation) becomes the focus.

- [x] **Hosted live demo at writtten.com** — **shipped 2026-07-10:** static PWA on Cloudflare Pages (`npm run build` → `dist/`, SPA fallback, custom domain, PWA offline shell). First-run is BYOK-only (proxy was a NO-GO): no key ⇒ the canned "See it in action" example auto-plays; a calm Settings prompt walks the visitor through pasting a free Gemini key to analyze their own doc. → see `docs/projects/oss_launch_readiness.md` (Good-enough)
- [x] **Pre-public review + placeholders** — placeholders **done 2026-07-05** (`<owner>`→`batirko` across README/CHANGELOG/issue-config; CoC + SECURITY contact → GitHub-only `@batirko`; README demo → writtten.com; redactions confirmed); **repo flipped public 2026-07-10**. The branch-protection residual is split out as its own item below. → see `docs/projects/oss_launch_readiness.md`
- [x] **Enable branch protection on `main`** (residual of the pre-public review) — GitHub's free tier only allows protection on a **public** repo; the repo is public now (verified unprotected 2026-07-10). **Shipped 2026-07-10:** classic branch protection rule on `main` — PR required before merging, `verify` required status check, branches must be up to date before merging; force-push and branch deletion left disabled. **Owner action**, done directly in GitHub Settings → Branches (not performable by the agent).
- [ ] **Hero visual** — a ~20–30s GIF of the loop (type planted-contradiction doc → card settles → reverse-hover highlights the span → click peeks the earlier claim), captured against the zero-config example at 1280×800, optimized ≤~5 MB, committed under `docs/launch/` and embedded at the top of the README. Storyboard + capture spec fixed. → see `docs/projects/oss_launch_readiness.md` (Good-enough) — 🟢 Low · ⚙️
- [ ] **In-app OSS discoverability** — surface that writtten is open source and link the repo from _inside_ the app, so a hosted-demo visitor (writtten.com) can find the source without leaving to the README. First cut: a GitHub repo link + a one-line "open source" note in Settings, next to the version tag; consider the empty state / footer as secondary spots. Small, but it's the in-product half of the OSS positioning that otherwise lives only in the repo. → see `docs/projects/oss_launch_readiness.md` (positioning) — 🟢 Low · 🔧
- [ ] **"SaaS doc baggage" — site legal & policy pages** — decide what a local-first, BYOK, no-backend static PWA actually needs (terms of use, a privacy statement, a short "no warranty" disclaimer), then write and link the minimum honest set. The privacy story is a _selling point_, not boilerplate: the document never leaves the browser except to the user's own model provider under their own key (Invariant 5) — say that plainly, and note the free-Gemini-tier egress caveat. Scope is deliberately _think-then-add_: figure out what we genuinely need as a non-data-collecting tool; don't cargo-cult a data-processing agreement we have no data for. → see `docs/projects/oss_launch_readiness.md` — 🟠 Med · 🧠
- [ ] **Real-model checks as a pre-release gate (CI blind spot)** (surfaced by the 2026-07-10 pre-launch sweep) — the demo-recording drift that shipped to prod was invisible to CI because every real-model check (`*.live.test.ts` — `live-check`, `eval:live`, `eval:v1`, tone) is key-gated and excluded from CI, so a retired model, a prompt/eval-quality regression, or a shipped-fixture drift can pass green CI and only surface in a production build. Make `npm run live-check` (real-provider reachability + Gemini pool liveness) a **manual pre-release gate**; optionally add a scheduled (non-CI, keyed) run. Complements the demo-drift guard now in CI (`exampleReplay.sync.test.ts`, #166). → see `scripts/live-check.sh` — 🟢 Low · 🔧
- [ ] **Strong-tier eval reliability** — bound strong-tier reasoning per adapter (OpenAI `reasoning_effort:'minimal'`, Gemini `thinkingBudget:0`; Anthropic already floors), raise the shared strong-tier request timeout 45s→60s, and give paid strong pools a failure-only fallback so a slow contradiction sweep no longer times out and drops the whole call. Prompted by a live `gpt-5.5` 503; it's the paid-BYOK path a converting demo visitor hits first. → see `docs/projects/strong_tier_eval_reliability.md` — 🟢 Med · ⚙️
- [ ] **Doc-level prompt: close the index/`§N` leak (OBS-034)** (pulled up from the prompt-quality log 2026-07-10 — prod-visible copy defect) — the doc-level pass leaks evaluator-internal claim indices ("claim [3]") and invents `§N` section numbers into user-facing card copy. Mirror the "never name a claim by its `[N]`/bookkeeping index — quote/restate instead" rule from the four contradiction prompts into `DOC_LEVEL_SYSTEM_PROMPT`, forbid `§N` addressing unless the document actually has numbered sections, and extend the `evalRatchet.test.ts` label-leak lint to doc-level `text` outputs so the class can't reopen on a third surface. Prompt-only (prompt edits re-key doc-level fixtures — re-key offline, byte-identical method). → see `docs/logs/prompt_quality_observations.md` (OBS-034) — 🟢 Low · 🧠
- [ ] **Doc-level Block Summaries must be document-ordered (OBS-035)** (pulled up from the prompt-quality log 2026-07-10 — prod-visible correctness bug) — the doc-level prompt feeds summaries/claims in **alphabetical** order (`evaluator.ts:703–705,710`), reusing the request-hash sort as the model's positional `[1]/[2]` input, so `structure_flow`/ordering observations reason over a scrambled sequence. Fix: keep the alphabetical order for `docStateHash`, but build the `Block Summaries`/`Claim Ledger` prompt sections from a separately document-ordered list. → see `docs/logs/prompt_quality_observations.md` (OBS-035) — 🟢 Med · ⚙️
- [ ] **Debug-log export redaction check** (split 2026-07-10 from `debug_log.md`'s Phase-5 scope as launch hygiene) — confirm API keys never leave the `<free>`/`<paid>` masking in the export envelope, including the `endpoint` field. The rest of that Phase-5 slice (log unify + token/cost + retention) is parked in Phase 9. → see `docs/projects/debug_log.md` — 🟢 Low · 🔧

---

## Phase 8 — Validation & signal quality

> The evidence phase — answer the strategic open questions (see _Discovered_) with data instead of desk reasoning, and drain the signal-quality follow-ups that either feed or ride the same keyed sessions. **V1 first** (decided with the owner 2026-07-10): the machinery, a stratified hand-labeled **21-doc** corpus, and human-verified labels already exist (`field_validation.md`; `docs/snapshots/2026-07-06_v1_base_rate_corpus_study.md`) — what remains is the keyed run + write-up, and it unblocks the per-type precision-floor recalibration, the free-vs-paid-tier decision, the decision-rigor taxonomy expansion (Phase 9), and three strategic open questions. V2 recruiting is easier while launch attention lasts. → see `docs/projects/field_validation.md`

- [ ] **V1 — Base-rate corpus study.** Run the pipeline over a real-PRD corpus; count un-planted true contradictions (hero base rate), per-type precision in the wild, and the free-vs-paid-tier delta. **Protocol settled 2026-06-18; machinery + stratified 21-doc corpus + human-verified labels landed 2026-07-06/07** — what remains is the keyed free+paid run (`V1_RECORD=1 npm run eval:v1`) and writing up the numbers (→ `docs/snapshots/2026-07-06_v1_base_rate_corpus_study.md`). Spec in `field_validation.md` § V1. — 🟢 Med · 🧠
- [ ] **V2 — External-PM sessions (×5)** on real in-flight drafts: observe write-vs-paste, whether located-critique lands as respect or coldness on a mature draft (the OBS-010 hypothesis), and whether anything drives a second session. **Protocol settled 2026-06-18:** unmoderated solo use (locally instrumented via harness event stream, no product telemetry) + a fixed-question debrief; recruit/consent/privacy spec'd. Spec in `field_validation.md` § V2. — 🟢 Med · 🧠
- [ ] **V3 — Hero-miss instrumentation.** A dev/eval-only way to measure contradiction-at-distance _recall_ (not just false positives) against V1's hand-labeled corpus, plus the Jaccard-prefilter drop count. **Design settled 2026-06-18:** extend the Tier-2 scorer to score recall against V1's labeling sheet (per bucket) + a with/without-prefilter A/B drop count; `EVAL_LIVE`-gated, no egress. Spec in `field_validation.md` § V3. — 🟢 Med–High · 🧠
- [ ] **Per-type precision floors — corpus growth + recalibration** (moved from Phase 6, 2026-07-08). The **Phase-6 tiered _gate_ shipped 2026-07-05** — `PRECISION_FLOORS` / `precisionFloorForType` / `AGGREGATE_RECALL_FLOOR` in `evalScorer.ts` (exhaustive `Record<Observation["type"],number>`); `evalRatchet.live.test.ts` asserts each covered type against its 4-tier trust-cost floor (A contradiction ≥0.95 · B unsupported_claim/audience_mismatch ≥0.85 · C clarity/undefined_jargon ≥0.80 · D opportunities ≥0.70), aggregate `recall ≥ 0.8`; CI-safe tier-policy unit tests. **What moved here is pure V1 work:** grow the corpus toward 20–40 cases (needs `npm run eval:record` live-recording) so the per-type floors gain statistical teeth, and replace author-only labels with V1's reconciled second-rater ground truth (which recalibrates the provisional constants). Gated on V1's keyed run. Spec in `evaluator_quality_ratchet.md` § Phase 6. → see `docs/projects/evaluator_quality_ratchet.md` (audit #7) · `docs/projects/field_validation.md` (V1) — 🟡 Med · 🧠
- [ ] **Free-tier (weak-model) signal-quality expectations** (surfaced by the 2026-07-10 keyed sweep on `GEMINI_FREE`). Weak-tier eval under-adjudicates: a planted cross-section Q1-vs-Q3 launch contradiction surfaced as a single-section `strategic_tension` rather than a cross-section `contradiction`, and clarity ran noisy (5 low-severity cards on a 178-word draft). Not a prod bug — expected free-tier behaviour (a paid key adjudicates more confidently) — but it sets the signal quality a keyless/free first-run user actually sees. Fold into V1's free-vs-paid-tier delta + the per-type precision-floor recalibration above. → see `docs/projects/field_validation.md` (V1 free-vs-paid delta) · `docs/projects/evaluator_quality_ratchet.md` — 🟡 Med · 🧠
- [ ] **Live-recording fixture session (merged decide-items, 2026-07-10)** — one keyed recording session (RPD budget; `gemini-2.5-pro` is RPD=0 on free tier) that pairs naturally with V1's keyed run resolves two formerly separate "Decide:" items: **(1) OBS-027 behavioural discrimination fixtures** — the shipped cross-section "Established elsewhere" context is guarded only at prompt-assembly level; record the four spec'd fixtures (cross-section reference resolved elsewhere must **not** flag; "Out of scope"/"Non-goals" heading suppressed by heading-intent; a genuine gap no sibling resolves must **still** surface; one further discrimination case) into the Tier-1 ratchet. **(2) OBS-031 contradiction message-fidelity fixture** — a live-recorded sweep fixture asserting the tightened prompt actually stops the "threshold → user segment" fabrication end-to-end (paraphrase drift is a model-output property deterministic replay can't prove). Per item the decision stands: record it, or explicitly accept the existing guard as sufficient. _(OBS-026's two-span refinement is **not** part of this — it's a build decision, not recording-blocked; parked in Phase 9.)_ → see `docs/projects/section_eval_precision.md` (OBS-027) · `docs/logs/prompt_quality_observations.md` (OBS-031) · `docs/projects/evaluator_quality_ratchet.md` — 🟡 Med · 🧠
- [ ] **Scope-excluded claim tagging (OBS-030)** — the cross-claim contradiction check has no signal that a claim's source section marked it out-of-scope, so an "Out of scope" item ("Multiple concurrent retries across devices.") surfaced a **High-confidence contradiction** against a real retry limit ("up to three times"). Same root cause as OBS-027, recurring in a different check. Fix: tag claims from "Out of scope"/"Non-goals" sections `scope: excluded` **at extraction time** (claim-ledger schema), and have the contradiction/sweep prompts skip or heavily downweight any conflict where either side is excluded. Split out of the Phase-6 message-fidelity PR (2026-07-06), which was prompt-only; this needs extraction-time tagging + a ledger field. → see `docs/logs/prompt_quality_observations.md` (OBS-030; "Scope-excluded claim tagging" Todo) · `docs/projects/section_eval_precision.md` (OBS-027) — 🟢 Med · 🧠
- [ ] **Essay/relaxed-class extraction calibration (OBS-036 residual + OBS-037)** (pulled from the prompt-quality log 2026-07-10) — two composing fixes that stop PRD-grade strictness cascading onto essays/blogs/memos: **(a)** extend the fast-tier first-person carve-out so interrogative/hypothetical framings of a personal concern ("won't my writing skill atrophy?") are caught alongside the declarative forms (OBS-036 residual — the calibration-floor softening half shipped in #162); **(b)** under relaxed document classes, stop promoting rhetorical/hyperbolic emphasis and narrative framing to ledger `fact_claim`s — the upstream source of the false `unsupported_claim` + `strategic_tension` cascade (OBS-037). Composes with `document_type_calibration.md`'s Phase-8 scope (per-class check applicability, class inference before the first strict pass). → see `docs/logs/prompt_quality_observations.md` (OBS-036, OBS-037) · `docs/projects/document_type_calibration.md` — 🟡 Med · 🧠
- [ ] **Cross-section short-draft contradiction gating (UX-016 residual)** — the paste sweep's doc-maturity/word-count gate still silences **cross-section** contradictions on short drafts (the intra-section case was closed for free by OBS-033 mechanism A, #161, since the per-section check isn't maturity-gated). Decide whether/how the sweep gate should relax for blatant conflicts on early drafts — the "impressive, structurally-critical check doesn't run when you'd most want it" failure. V1/V2 evidence on early-draft behaviour should inform the gate. → see `docs/logs/ux_quality_observations.md` (UX-016) · `docs/projects/contradiction_coverage.md` — 🟠 Med · 🧠

---

## Phase 9 — Later / optional (post-traction)

Only if the drafting habit has taken hold. Don't pre-build any of this — each item stays parked until its trigger fires (traction, V1/V2 evidence from Phase 8, contributor interest, or funnel data).

- **Decision-rigor taxonomy expansion** — `unstated_assumption`, `alternatives_not_considered`, `unmeasurable_criteria`, `scope_ambiguity`, `ownerless_commitment`. **Research-gated:** the real-PRD corpus study this waits behind is **V1** (Phase 8 — `docs/projects/field_validation.md`); the taxonomy expansion stays downstream of that evidence. → see `docs/projects/observation_taxonomy_and_priority.md` (Milestone C) — 🟠 High · 🧠
- **Reflection / document-mirror kind** — client-side, zero LLM calls, quiet separate panel. → see `docs/projects/observation_taxonomy_and_priority.md` (Milestone D) — 🟢 Med · ⚙️
- **BYOK multi-key rotation + management UX** (merged 2026-07-10 from two adjacent entries) — the rotation half: pool entries become `{key, model}`, cool-down registry keyed by `key+model`, **N free keys** for RPD spreading; additive, contained in `gemini.ts` (the free+paid dual-key pair shipped 2026-07-07). The UX half it enables: let the user add several keys, **see which models those keys give them and how we route each** (fast vs strong / which check uses which), and optionally override the routing. The capability descriptor (`capability.ts`) already decouples credential from capability, and the provider question the old entry left open is **resolved** — Gemini + OpenAI + Anthropic all shipped with per-provider model selection (multi_provider_router, 2026-07-07) — so what's left is the key-inventory / routing-transparency design over an existing seam. Stays client-side (invariant 5). → see `docs/projects/byok_capability_model.md` (Phase 9) — 🟠 Med–High · 🧠
- **Decide: OBS-026 real-offset two-span disambiguation (section-eval precision follow-up)** — the shipped OBS-026 fix surfaces same-block contradictions as a **single whole-block highlight** (detection restored; the card names both claims). The deferred refinement computes **real per-claim offsets** by substring-matching each claim's text within the block, restoring a true two-span A↔B highlight and fixing the v1 limitation where two _distinct_ intra-section conflicts in one section collapse into one card (shared `[block, block]` pair key + `0:9999` content-sig). Fragile substring matching (a reworded claim isn't a verbatim substring); couples with the `doc_level_anchoring` substring resolver. **Explicitly rated "not the floor"** — OBS-026's value was trust-defense (stop silently dropping detected conflicts), not the phrase-level highlight. Decide whether the two-span highlight earns the code + fragility, or stays deferred. → see `docs/projects/section_eval_precision.md` (OBS-026, § Deferred) — 🟠 Med · ⚙️
- **Smart-feed-vs-manual-control (R2c) — the one conceded noisiness switch** — the three-step **Key issues / Balanced / Everything** volume dial over the priority budget (`observation_taxonomy_and_priority.md` Milestone E: the `NOISINESS` map, `FeedPartitionOptions.noisiness`, `localStorage["writtten_noisiness"]`, the three-segment control, `feedBudget.test.ts` cases; must compose with the G4 contradiction floor/ceiling). Design + build spec are **complete — no new design needed**; per-type filters / sorting / manual "top N" deliberately refused. **Build-held pending V2 (Phase 8):** if V2 shows maturity-aware curation (now live) already dissolves the UX-010 friction, we never add the control (the more zero-config outcome). → see `docs/projects/smart_feed_curation.md` · `docs/projects/observation_taxonomy_and_priority.md` (Milestone E) · `docs/projects/maturity_aware_severity.md` (R2) · resolves UX-010 — 🟡 Med · 🧠
- **Mobile review companion** — the real touch interaction model for the span↔card relationship, reframed to fit the product: on a phone you _review_ observations (feed as a bottom sheet / tab, tap-a-span → its card rises, tap-a-card → editor scrolls+flashes the span), you don't thumb-type a PRD. A genuine second interaction surface, downstream of the Phase-6 courtesy pass; same underlying gap as `accessibility.md`'s keyboard/AT equivalence for the hover hero interaction (both give span↔card a non-hover driver). Don't pre-build. → see `docs/projects/mobile_responsive.md` (Phase 7) — 🟠 High · 🧠
- Living _where users already write_ (Notion / Linear / Confluence / email) instead of being a drafting annex — the real long-term play, per `docs/concept.md`. — 🔴 Very High · 🧠
- **Superb tier — contributor depth (post-launch OSS on-ramp).** `docs/extending.md` documenting the three seams — model providers (`ProviderAdapter`), observation types + per-type prompts (within the fixed-taxonomy invariant), export formats (`export.ts`) — each with _where it lives / what to implement / a worked example_; plus a curated good-first-issue list (Ollama/local adapter, a new export format, docs sweep, signal-quality FP/FN), contribution-guardrail docs, and the post-launch triage cadence. Specced to 🟢, deferred out of the launch (2026-07-06) to accrete as contributors arrive. The non-Gemini adapter that once anchored this now ships first-party _at_ launch (multi_provider_router.md), so this reduces to the docs + issue-pipeline layer. → see `docs/projects/oss_launch_readiness.md` (Superb) — 🟢 High · 🧠
- Optional Tauri desktop wrapper — the "proper local app" path. Mostly _extend-not-rewrite_: the UI runs in the system webview and persistence + model-router are already sealed seams; the main new surface is multi-document and a SQLite/filesystem backend behind `db.ts`. The Tauri-vs-integrations fork and the two enabling invariants (idb sealed; `docId` never re-hardcoded) are documented in `docs/architecture.md` → _Local-app evolution path_; invariant 1 is enforced by an ESLint rule. — 🟡 Med · ⚙️
- **Hosted zero-config proxy (revisit).** The capped, shared-key Gemini proxy that would let writtten.com visitors evaluate their own writing with no key — **decided NO-GO for launch (2026-07-06)**; the launch demo is BYOK-only. Fully specced and build-ready (hard global daily budget + per-client throttle; a paid-key proxy is a privacy _upgrade_ over free BYOK). Revisit post-traction **if** the BYOK-only demo shows first-use friction is costing conversions — the trigger is funnel evidence, not missing design. The monetization sibling below (managed model access) is the paid framing of the same seam. → see `docs/projects/hosted_proxy.md` — 🟢 Med · 🧠
- Lightweight monetization exploration (hosted convenience / managed model access on an OSS core) — only if traction warrants it. — 🔴 N/A · 🧠
- **Agent/harness-connected eval source — write the spec (bring-your-own-agent, no API key)** (promoted 2026-07-10 from concept to a spec-writing milestone; **build stays ungated/undecided**) — an alternative to the BYOK model-router path: instead of writtten holding a key and calling a provider, an external coding-agent session (Claude Code, Codex, etc.) _connects to the writtten document_ and becomes the source of observations — the bring-your-own-agent mechanism [proofeditor.ai](https://proofeditor.ai) uses (paste a skill into the agent once; presence/identity instead of API keys; the agent reads the shared doc and emits into it). Attractive because it inherits the user's _existing_ model access (no key to paste, no writtten-side egress — a privacy + on-ramp win) and could ride the seam we already have: `window.__sidecar__` exposes `loadLedger` + observation emit today, so this is the productized, non-dev version of that bridge. **The load-bearing constraint the spec must resolve:** proofeditor lets the agent _suggest edits and leave comments_ — writtten's Invariant 1 forbids exactly that, so a connected agent must be bound to the fixed observation taxonomy (emit typed observations, never edits/rewrites/apply-affordances); whether an external, general-purpose agent can be held to _provoke, don't prescribe_ is the real design risk, not the transport. Deliverable: a `docs/projects/` spec covering bridge protocol, taxonomy + register enforcement at the boundary, and the trust/attribution model. → relates to `docs/projects/byok_capability_model.md` · `docs/projects/hosted_proxy.md` · `docs/projects/agent_acceptance_harness.md` — 🟠 Med (spec only) · 🧠
- **Debug-log unify + token/cost + retention** (the rest of `debug_log.md`'s Phase-5 slice; the redaction check rides Phase 7) — a single emitter feeding both the agent event stream and the human debug panel (reconcile the two type vocabularies and retention limits); capture Gemini `usageMetadata` (prompt/candidate token counts) per call and surface per-call cost alongside latency; retention review against a realistic bulk-paste session. Dev-infra QoL; nothing user-facing blocks on it. → see `docs/projects/debug_log.md` (Phase 5) — 🟡 Med · ⚙️
- **L8 — Editor hot-path perf** (per-keystroke O(doc) `resolveSections`/`refreshObservations` full scans; Phase 5 carryover). Profile first; schedule only if dogfooding shows real lag on long docs. → see `docs/projects/lifecycle_integrity.md` (L8) — 🟠 Med · ⚙️
- **Docs maintenance — archive `done` projects** — **closed-phase half shipped 2026-07-10:** Phases 0–6 moved to [`plan-archive.md`](plan-archive.md) behind recap stubs. **Remaining — the `done` project files:** the Projects Index carries 20+ `done` rows. Deferred deliberately (2026-07-10) because many `done` specs are still referenced by open items, so archiving the _files_ would force fragile live→`archive/` cross-links; revisit once those specs go inert. Test-aware options when scheduled: move `done` files to `docs/projects/archive/` (non-recursive `readdirSync` drops them from the completeness contract automatically — but rows must also leave the index, and inbound links need rewriting) **or** extend `projects.index.test.ts` to accept an archived grouping/`archive:` frontmatter flag. → see [`plan-archive.md`](plan-archive.md) — 🟡 Med · ⚙️

## Discovered / unscheduled

> Insights from real test sessions not yet scoped into a phase. Triage each into a phase or discard — don't let them rot here. Source reviews live in `docs/snapshots/`. _(The 2026-06-03 signal-quality review's items — jargon allow-list, `strategic_tension`, aggregation, impact badging — have been triaged into Phase 4 above.)_

> Anticipated-but-unscheduled items below were surfaced by the 2026-06-06 scope analysis from project-doc open-questions / deferred decisions. Each is `(deferred)` (a path chosen against, revisit on a trigger) or an `(open question)` (a decision not yet made). Routing annotations apply as above.

- **LEANN real-vector prefilter** `(deferred)` — shipped as a lexical Jaccard prefilter; real embeddings deferred (Python dep / WASM bundle weight). Revisit if claim density makes contradiction-check misses observable in practice. **The 2026-06-10 due-diligence audit (#2) flags a hidden cost:** the lexical top-10 silently drops semantically-related-but-lexically-distant pairs ("Q2" vs "the second quarter", "20% lift" vs "one in five"), eroding hero-recall invisibly — `field_validation.md` V3 quantifies the drop and is the trigger to revisit. → `docs/projects/ai_tooling_integration.md` — 🟠 · 🧠
- **markitdown binary import (DOCX/PDF)** `(deferred)` — held to preserve the local-first invariant; MD/TXT-only import shipped. Needs the WASM-port-vs-optional-local-helper decision before scoping. → `docs/projects/ai_tooling_integration.md` — 🟠 · 🧠
- **Ollama / local-model adapter** `(open question — merged 2026-07-10)` — merges the old "non-Gemini provider adapters" bullet (mostly resolved: OpenAI + Anthropic shipped first-party 2026-07-07 via `multi_provider_router.md`) with the "pull the Ollama adapter earlier?" strategic question below. The remaining case is **local**: on the free tier, confidential PRDs are shipped to a provider whose terms may permit training on them, so "local-first / privacy-respecting" (Invariant 5) is currently a claim about _storage_ while every settled block leaves the machine — a local adapter is the only thing that makes "no-egress privacy" true end-to-end, and the Phase-7 privacy pages will be _advertising_ that story. Now much cheaper than when first raised: the `ProviderAdapter` seam exists, so this is a scoped adapter (plus capability-tier honesty for small local models), and it doubles as the anchor good-first-issue for the contributor on-ramp. Triggers: contributor interest, or privacy-driven demand post-launch. → `docs/projects/multi_provider_router.md` · `docs/projects/oss_launch_readiness.md` (Superb) — 🟡 · ⚙️
- **Priority decay over session time** `(open question)` — gently decay an undismissed observation's priority so the feed stays fresh. UX refinement; don't build without dogfooding evidence that stale-but-undismissed cards are a real problem. → `docs/projects/observation_taxonomy_and_priority.md` — 🟠 · ⚙️
- **Reflection tone-shift detection** `(deferred)` — needs per-section tone metadata in the block-summary schema first; do not build a speculative per-section tone call. → `docs/projects/observation_taxonomy_and_priority.md` — 🟠 · 🧠
- **`ownerless_commitment` as a client-side regex check** `(open question)` — `commitment` claims are already in the ledger; a lightweight no-LLM scan may suffice if the false-positive rate is acceptable. Validate against corpus. → `docs/projects/observation_taxonomy_and_priority.md` — 🟡 · ⚙️
- **Inject dismissals into the prompt (vs. local-only filtering)** `(open question)` — dismissal is currently **100% local post-LLM filtering**: `isSpanSuppressed` (`evaluatorReconcile.ts:67`) drops re-emitted matches before insert; G1 makes it severity-aware (high-severity / `contradiction` / `unsupported_claim` → span-only; low/med → category-wide for the doc). The model **never sees suppressions** — so it re-generates dismissed observations on every eval and we discard them (wasted tokens; no "learning"). `message_generation_workflow.md` §1/§9 _describe_ injecting a "do not re-raise" line into the system prompt, but that was never built (doc drift, corrected 2026-06-17). Decide whether to actually inject suppressions (the promised behavior + a token saving on the fast path) or keep local-only (simpler, model-agnostic, no prompt-bloat). Local-only is defensible; the drift is the bug. → `docs/projects/message_generation_workflow.md` (§9) — 🟡 · 🧠

> **Strategic open questions** (2026-06-10 due-diligence audit). These are decisions, not features — they want **evidence, not reasoning** ("naming a risk in beautiful prose feels like handling it"). The `field_validation.md` track (V1/V2) is how they get answered; don't resolve them at a desk. **That track is Phase 8 (scheduled 2026-07-10, V1 first), so these stay open until it runs.** _(Two earlier questions left this list: "OSS: real or decorative?" resolved → real, 2026-07-05 — record in `oss_launch_readiness.md`; the Ollama/local-adapter question merged into the adapter bullet above, 2026-07-10.)_

- **Free tier: real tier or demo?** `(onboarding half resolved 2026-07-07 → demo; quality delta still open)` — **Resolved for first-run:** with **no** key the evaluator skips every check (`evaluator.ts:181`, no bundled key), so keyless produces zero analysis on the user's own writing — keyless is a **demo**, and the first-run now says so (blocking welcome modal + standing "add a key" card; → `onboarding_first_run.md` § Revision, and the _First-run activation_ Phase-6 milestone). **Still open (the quality half):** even _with_ a free-tier key, the binding constraint is ~20 RPD per Flash model and **0** for `gemini-2.5-pro`, so free-tier "strong" checks run on a weak model and emit confident false contradictions (the R4.4 failure that discounts the whole feed) — whether a free-tier key meets the fidelity bar (vs. paid being effectively mandatory) is the V1 free-vs-paid delta. Cross-ref the "free-tier contradiction strategy" open question in `evaluation_signal_quality.md`. → `docs/projects/field_validation.md` — 🟠 · 🧠
- **Paste-first vs ambient-companion thesis** `(open question)` — if real usage is paste → read → leave, the settling/rhythm/lifecycle machinery (much of R3/R5) services a loop users don't inhabit, and what survives is "LLM document reviewer with great span anchoring." The defensible thesis is the ambient companion; this asks whether it survives contact with the persona's actual workflow. Evidence: V2 (do they write or paste?). → `docs/projects/field_validation.md` — 🔴 · 🧠
- **Does maturity-aware severity (R2) dissolve the OBS-010 discomfort?** `(open question)` — the "abrasiveness was about _when_, not _what_" reframe is a good hypothesis promoted to a conclusion without a test; it's also exactly the reasonable-sounding move that lets the project avoid confronting whether "locate, don't prescribe" survives a tired user at 5pm. Links the unstarted R2c smart-feed design. Evidence: V2 (register-compliant critique on a mature draft — respect or cold?). → `docs/projects/quality_remediation_synthesis.md` (R2) · `docs/projects/field_validation.md` — 🟡 · 🧠

---

## Standing rules across all phases

1. **No fix-application affordances. Ever.** (The product principle — see `CLAUDE.md`.)
2. Observations stay within the **fixed, typed taxonomy**.
3. **No per-keystroke full-document scans;** cross-doc checks go through the claim ledger.
4. **Quiet while generating, opinionated while revising.**
5. **Local-first / privacy** — no required server, telemetry, or egress without a decision logged in this file.
