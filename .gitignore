node_modules
dist
# All local env files (.env.local, .env.test.local, …) — never commit keys.
.env*.local
# Live-model test keys, if dropped in the repo root instead of ~/.config/writtten/.
test-keys.env
*.tsbuildinfo

# Scratch / captured test output (keep out of the public repo)
*_output.txt

# Obsidian vault config (rich-text editing of docs/ — local, per-machine)
.obsidian/

# Per-session git worktrees for parallel feature branches (local, per-machine)
.worktrees/

# V1 base-rate corpus study: real PRDs + filled labeling sheets + dumped
# recordings stay LOCAL (local-first invariant #5; audit-#5 privacy). Only the
# templates + loader under src/ are committed. See
# docs/projects/field_validation.md § V1.
.v1-corpus/
